// MARK: - Images

/// An object that defines all the images for Habanero.
public protocol Images {
    /// Add image.
    var add: UIImage? { get }
    /// Bell image.
    var bell: UIImage? { get }
    /// Briefcase image.
    var briefcase: UIImage? { get }
    /// Camera image.
    var camera: UIImage? { get }
    /// Caret down image.
    var caretDown: UIImage? { get }
    /// Caret left image.
    var caretLeft: UIImage? { get }
    /// Caret left thick image.
    var caretLeftThick: UIImage? { get }
    /// Caret right image.
    var caretRight: UIImage? { get }
    /// Caret right thick image.
    var caretRightThick: UIImage? { get }
    /// Caret up image.
    var caretUp: UIImage? { get }
    /// Check circle image.
    var checkCircle: UIImage? { get }
    /// Check small image.
    var checkSmall: UIImage? { get }
    /// Clipboard image.
    var clipboard: UIImage? { get }
    /// Close image.
    var close: UIImage? { get }
    /// Close circle image.
    var closeCircle: UIImage? { get }
    /// Close circle fill image.
    var closeCircleFill: UIImage? { get }
    /// Edit image.
    var edit: UIImage? { get }
    /// Email image.
    var email: UIImage? { get }
    /// Empty image.
    var empty: UIImage? { get }
    /// Error image.
    var error: UIImage? { get }
    /// Face smile image.
    var faceSmile: UIImage? { get }
    /// Folder image.
    var folder: UIImage? { get }
    /// Gear image.
    var gear: UIImage? { get }
    /// Heart plus image.
    var heartPlus: UIImage? { get }
    /// Help image.
    var help: UIImage? { get }
    /// Info image.
    var info: UIImage? { get }
    /// Light bulb image.
    var lightBulb: UIImage? { get }
    /// Lock image.
    var lock: UIImage? { get }
    /// Logout image.
    var logout: UIImage? { get }
    /// Menu image.
    var menu: UIImage? { get }
    /// Money image.
    var money: UIImage? { get }
    /// Notes image.
    var notes: UIImage? { get }
    /// Parking image.
    var parking: UIImage? { get }
    /// Phone call image.
    var phoneCall: UIImage? { get }
    /// Profile image.
    var profile: UIImage? { get }
    /// Search image.
    var search: UIImage? { get }
    /// Ticket image.
    var ticket: UIImage? { get }
    /// Time clock image.
    var timeClock: UIImage? { get }
    /// Uniform image.
    var uniform: UIImage? { get }
    /// User image.
    var user: UIImage? { get }
    /// Warning image.
    var warning: UIImage? { get }
}
